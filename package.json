{
  "name": "anydeee",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "check:json": "node -e \"JSON.parse(require('fs').readFileSync('vercel.json','utf8')); JSON.parse(require('fs').readFileSync('site.webmanifest','utf8')); console.log('json ok')\""
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=20"
  }
}
